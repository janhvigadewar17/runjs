// Two types of datatypes 
// primitive and Non-primitive
 /* *****primitive****8 -> all are call by value, a change happens in the copy itself
   String 
   Number
   Boolean
   Null
   Undefined
   Symbol
   BigInt

//*******Reference (Non-primitive)******* types -> memory me reference allocate kr skte hai, all of them have return type as function

  Array, Objects, Funcitons

////////Javascript is a dynamically typed language
  */ 

const heros = ["shaktiman", "naagraj", "doga"];

let myObj = {
    name: "hitesh",
    age: 22,
}

const myFunction = function(){
        console.log("Hello WOrld");
 }